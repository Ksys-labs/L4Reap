-- vim:set ft=lua:

-- loader = L4.default_loader;

loader = L4.Loader.new({factory=L4.Env.factory, mem=L4.Env.mem_alloc});
print("ksys_mem_map_rwx started ...");
e = loader:start(
  {
	caps = {
	},
	log      = { "log_ksys", "g" },
	l4re_dbg = L4.Dbg.All,
  }, "rom/ksys_mem_map_rwx");
print("map_rwx exited with: " .. e:wait());
print("");

